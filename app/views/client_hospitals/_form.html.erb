<%= form_with(model: client_hospital) do |form| %>
  <% if client_hospital.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(client_hospital.errors.count, "error") %> prohibited this client_hospital from being saved:</h2>

      <ul>
        <% client_hospital.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :doctor_id, style: "display: block" %>
    <%= form.text_field :doctor_id %>
  </div>

  <div>
    <%= form.label :patient_id, style: "display: block" %>
    <%= form.text_field :patient_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
